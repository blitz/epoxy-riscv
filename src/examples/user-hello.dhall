-- An application description is a set of processes and their capabilities.
{ kobjects =
    [ { gid = 0, kobjType = "KLOG" } ]
, processes =
    [ { pid = 0, binary = "hello.user.elf", capabilities = [ 0 ] } ]
}
